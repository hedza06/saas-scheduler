spring:
  application:
    name: Saas Scheduler
  profiles:
    active: dev
  liquibase:
    change-log: classpath:liquibase/master.xml

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /scheduler-saas.html